import { EditCompanyInfo } from './edit-company-info';

describe('EditCompanyInfo', () => {
  it('should create an instance', () => {
    const directive = new EditCompanyInfo();
    expect(directive).toBeTruthy();
  });
});
